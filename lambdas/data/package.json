{
  "name": "@michitson/agentcore-data-lambda",
  "version": "0.1.0",
  "private": true,
  "description": "AgentCore Gateway Lambda target exposing CRE market reference data (cap rates + comparable sales). Synthetic dataset; Athena-backed later.",
  "type": "module",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "emit-schema": "npm run build && node scripts/emit-tools.mjs"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
